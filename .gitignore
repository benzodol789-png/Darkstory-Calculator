# ---------------------------------------------------------------------------
# repo นี้เป็นสาธารณะ — อะไรที่ push ขึ้นไปครั้งเดียวถือว่าหลุดถาวร
# ---------------------------------------------------------------------------

# ลิงก์ Apps Script ตัวจริงของกิลด์ ใครได้ไปเขียนทับชีตได้เลย
# (ตัวอย่างที่ไม่มีความลับคือ darkstory_config.example.json ซึ่งขึ้น repo ได้)
darkstory_config.json

# ลิงก์ที่ฝังไปกับ .exe — อยู่ในเครื่องเท่านั้น ไม่ขึ้น repo สาธารณะ
darkstory_defaults.py

# ของที่ PyInstaller สร้างระหว่าง build
build/
dist/
release/

# ไฟล์ .exe ที่แจกเพื่อน — ใหญ่เกินไป และควรอยู่ใน Releases ไม่ใช่ใน repo
*.zip
*.exe

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# ล็อกและค่าที่เก็บในเครื่องของแต่ละคน
*.log
darkstory_local.json

# ระบบปฏิบัติการ / เอดิเตอร์
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# โปรเจกต์ Android — สร้างใหม่ได้จาก android/twa-manifest.json
# ยกเว้นกุญแจเซ็นแอป ซึ่งห้ามหายและห้ามหลุด (ดู README)
android/android.keystore
android/app/
android/build/
android/gradle/
android/gradlew*
android/.gradle/
android/build.gradle
android/settings.gradle
android/gradle.properties
android/store_icon.png
android/manifest-checksum.txt
*.apk
